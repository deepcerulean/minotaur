module Minotaur
  module Pathfinders
    #
    #   A randomly-moving mouse should *always* (eventually) be able to find their
    #   way to the exit.
    #
    module RandomMouse
      #def self.unexplored_passable_adjacent

      # keep moving in current direction until you
      def path_between(origin,destination=Position.origin,labyrinth=self) #,path=[]) #,path=[])


        #current = origin
        #direction = labyrinth.each_
        #while current != destination
        #
        #end

        #puts "--- Attempting to find distance between origin #{origin} and destination #{destination} for labyrinth"
        #puts labyrinth
        ##path = []
        #path << origin
        #return path if origin==destination
      end

    end
  end
end